# SPEC

Our Budget의 기능 명세서다. README가 "무엇이 있는지" 소개하는 문서라면, 이 문서는 "각 기능이 실제로 어떻게 동작하는지"를 `index.html` 코드 기준으로 정리한다. 데이터 구조·저장 흐름·Firestore 동기화의 내부 동작은 [DATABASE.md](DATABASE.md)에 있으므로 여기서는 반복하지 않고 필요할 때 링크로 참조한다.

## 1. 거래내역

**목적**: 개별 지출을 날짜/카테고리/금액/구분 단위로 기록한다.

**입력**: 두 가지 경로가 있다.
- 거래 표 맨 아래 빠른 입력 줄: 날짜(`quickTxDate` + 달력 아이콘), 대분류(`openCatPicker`로 여는 픽커), 중분류(`quickTxMid`), 금액(`quickTxAmount`), 구분(`openWhoPicker`로 여는 픽커) → `submitQuickTx()`
- 상단 "+" 버튼 → 모달(`openAddModal('tx')`): 날짜, 대분류, 중분류, 소분류(메모), 금액, 구분(다혜/동구/공동 알약 버튼) → `submitAddModal()`

**처리 과정**: 날짜는 `normalizeDateStr()`로 `"MM월 DD일"` 형식으로 통일된다. 필수값은 날짜·대분류·금액이며, 하나라도 비어 있으면 `alert('날짜, 대분류, 금액은 꼭 입력해줘')`를 띄우고 저장을 중단한다. 저장에 성공하면 입력한 날짜가 속한 월을 자동으로 조회 월로 전환한다.

**저장 방식**: `store.transactions`에 새 객체를 `push`한 뒤 `save()`를 호출한다.

**수정**: 세 가지 경로가 있다.
- 행 전체 수정: 연필 아이콘(`editTx(i)`) → 행이 입력 필드로 바뀌고, 저장(`saveEditTx(i)`)은 추가와 같은 필수값 검사를 거쳐 `store.transactions[i]` 객체를 통째로 교체한다.
- 표에서 카테고리 배지를 바로 클릭(`openCatPicker(null, i, this)`) → 다른 카테고리 선택 즉시 반영, 별도 저장 버튼 없이 바로 `save()`.
- 표에서 구분 배지를 바로 클릭(`openWhoPicker('tx', i, this)`) → 다혜/동구/공동 선택 즉시 반영, 바로 `save()`.
- 메모 아이콘(`openMemoPicker('tx', i, this)`) → 메모(`detail`)만 별도로 수정, 저장 시 바로 `save()`.

**삭제**: 삭제 아이콘(`deleteTransaction(i)`) → 확인창 없이 즉시 `store.transactions`에서 `splice`로 제거하고 `save()`.

**예외 처리**: 필수값 누락 시 `alert`만 띄우고 중단한다. 그 외 별도의 오류 처리(try/catch 등)는 없다.

## 2. 수입

**목적**: 수입 내역을 기록해 "이번 달 저축 현황" 계산의 기준으로 쓴다.

**입력**: 수입 내역 섹션의 "+" 버튼 → 모달(`openIncomeModal()`): 날짜, 내용, 금액, 구분(다혜 기본 선택) → `submitIncomeModal()`.

**처리 과정**: 날짜는 `normalizeDateStr()`로 통일한다. 필수값은 날짜와 금액뿐이며 **내용(이름)은 필수가 아니다**. 저장 성공 시 해당 월로 조회 월이 전환된다.

**저장 방식**: `store.incomes`에 `push` 후 `save()`.

**수정**: 연필 아이콘(`editIncome(i)`)으로 날짜/내용/금액을 인라인으로 고치고 `saveEditIncome(i)`가 날짜·금액 필수값을 검사한 뒤 `store.incomes[i]`를 교체한다. 구분(다혜/동구/공동)은 이 인라인 폼이 아니라 표의 구분 배지를 직접 클릭(`openWhoPicker('income', i, this)`)해서 바꾼다.

**삭제**: `deleteIncome(i)` → 확인창 없이 즉시 삭제.

**예외 처리**: 날짜·금액 누락 시 `alert('날짜, 금액은 꼭 입력해줘')`.

## 3. 고정지출

**목적**: 매달 반복되는 고정 지출 항목(통신비, 보험 등)을 관리한다.

**입력**: "+" 버튼 → 모달(`openAddModal('fixed')`): 결제일(자유 텍스트, 예: `10일`, `말일`), 내용, 금액, 구분 → `submitAddModal()`.

**처리 과정**: 필수값은 내용과 금액이며, 결제일은 비워둘 수 있다. 결제일은 날짜 파서를 거치지 않고 입력한 문자열 그대로 저장된다.

**저장 방식**: `store.fixedExpenses`에 `push` 후 `save()`.

**수정**: `editFixed(i)`로 결제일/내용/금액/구분을 인라인으로 고치고, `saveEditFixed(i)`가 내용·금액 필수값을 검사한 뒤 객체를 통째로 교체한다.

**삭제**: `deleteFixed(i)` → 확인창 없이 즉시 삭제.

**예외 처리**: 내용·금액 누락 시 `alert('내용, 금액은 꼭 입력해줘')`. 화면에 표시될 때는 결제일 기준으로 정렬되는데, `말일`은 숫자가 아니므로 항상 맨 마지막에 오도록 별도 처리돼 있다(저장 순서 자체가 바뀌는 것은 아니고 화면에 보여줄 때만 재정렬).

## 4. 저축·투자

**목적**: 저축·투자 항목을 관리한다.

**입력**: "+" 버튼 → 모달(`openAddModal('saving')`): 일자(자유 텍스트), 내용, 금액, 구분 → `submitAddModal()`.

**처리 과정**: 고정지출과 동일한 형태(일자/내용/금액/구분)로 저장되며, 필수값도 동일하게 내용과 금액이다.

**저장 방식**: `store.savings`에 `push` 후 `save()`.

**수정**: `editSaving(i)` / `saveEditSaving(i)`로 인라인 수정.

**삭제**: `deleteSaving(i)` → 확인창 없이 즉시 삭제.

**예외 처리**: 내용·금액 누락 시 `alert('내용, 금액은 꼭 입력해줘')`. 고정지출과 달리 저축·투자 목록은 화면에서도 별도의 정렬 로직 없이 저장된 순서 그대로 보여진다.

## 5. 자산현황

**목적**: 다혜·동구·공동 명의 자산을 소유자·현금화 가능 여부와 함께 관리하고, 시점별로 스냅샷을 남겨 비교한다.

**입력**: "+ 자산 추가" 버튼 → 모달(`openAssetModal()`): 자산명, 소유자(동구/다혜/공동), 금액, 현금화(가능/불가), 메모(선택) → `submitAssetModal()`.

**처리 과정**: 필수값은 자산명과 금액이다. 저장에 성공하면 `store.assetAsOf`(자산 기준일)가 오늘 날짜(`YYYY-MM-DD`)로 갱신된다.

**저장 방식**: `store.assets`에 `push` 후 `save()`.

**수정**: `editAsset(i)`로 자산명·금액·현금화 여부를 인라인으로 고친다. **소유자와 메모는 이 수정 폼에 없다** — `saveEditAsset(i)`는 기존 `owner`와 `memo` 값을 그대로 유지한 채 나머지만 교체한다. 메모만 따로 고치려면 메모 아이콘(`openMemoPicker('asset', i, this)`)을 쓴다. 수정·메모 변경 모두 `assetAsOf`를 오늘 날짜로 다시 갱신한다.

**삭제**: `deleteAsset(i)` → 확인창 없이 즉시 삭제, `assetAsOf`도 함께 갱신.

**예외 처리**: 자산명·금액 누락 시 `alert('자산명, 금액은 꼭 입력해줘')`. 스냅샷 저장(`saveAssetSnapshot()`) 시 저장할 월을 비워두면 `alert('스냅샷으로 저장할 월을 입력해줘 (예: 7)')`를 띄우고 중단한다. 같은 월로 다시 저장하면 기존 스냅샷을 덮어쓴다. **과거 스냅샷을 조회하는 중에는 자산 추가·수정·삭제가 불가능하다** — "+ 자산 추가" 버튼이 숨겨지고 각 행의 수정·삭제 버튼도 표시되지 않는다(별도 오류 메시지 없이 버튼 자체가 사라지는 방식).

## 6. 목표 자금 계획

**목적**: 목표(예: 주택 매매)에 필요한 자금과 이미 마련된 돈을 각각 정리해서 달성률과 월 저축 필요액을 자동 계산한다.

**입력**: 목표명(`goalTargetLabel`)과 목표 시점(`goalTargetDate`, 월 단위)은 입력창에 값을 바꾸면 바로 반영된다(`onGoalLabelChange` / `onGoalDateChange`). 필요 자금 항목은 표 하단 인라인 폼(`goalCostName` + `goalCostAmount` → `addGoalCost()`)으로, 차감 항목(이미 마련된 돈)은 별도 인라인 폼(`goalDeductionName` + `goalDeductionAmount` → `addGoalDeduction()`)으로 추가한다.

**처리 과정**: 항목 추가 시 필수값은 항목명과 금액이다. 목표명은 비워서 저장하면 자동으로 `'목표'`로 채워진다. 계산은 다음 순서로 이뤄진다.
- 필요 자금 합계 − 차감 합계 = 최종 모아야 할 돈(0 미만이 되지 않도록 보정)
- 최종 모아야 할 돈 − 현재 현금화 가능 자산 = 부족한 금액(마찬가지로 0 미만 방지)
- 목표 시점까지 남은 개월 수(`monthsUntilGoal`)로 부족한 금액을 나눠 월 저축 필요액을 계산(올림 처리)
- 필요 자금이 아직 없거나, 목표 시점이 없거나 이미 지났으면 월 저축 필요액 대신 안내 문구("목표를 입력해줘" 등)를 보여준다.
- 달성률은 현재 현금화 가능 자산 ÷ 최종 모아야 할 돈으로 계산하고, 이미 최종 모아야 할 돈을 넘어섰다면 초과 달성 금액을 대신 보여준다.

**저장 방식**: `store.goal`의 `costItems` / `deductionItems` 배열에 `push` 후 `save()`.

**수정**: `editGoalCost(i)` / `editGoalDeduction(i)`로 인라인 수정, 저장 시 항목명·금액 필수값을 검사한다.

**삭제**: `deleteGoalCost(i)` / `deleteGoalDeduction(i)` → 확인창 없이 즉시 삭제.

**예외 처리**: 항목명·금액 누락 시 `alert('항목명, 금액은 꼭 입력해줘')`. 그 외 계산 관련 예외(필요 자금 0원, 목표 시점 미입력/경과)는 오류창이 아니라 카드에 안내 문구로만 표시된다.

## 7. CSV 가져오기

**목적**: 여러 건의 거래 내역을 CSV 파일이나 엑셀에서 복사한 셀 범위로 한 번에 등록한다. 줄 단위 파싱 규칙과 컬럼 매핑 상세는 [DATABASE.md의 "9. CSV 가져오기"](DATABASE.md)에 있으므로 여기서는 반복하지 않는다.

**입력**: "⇪ 엑셀/CSV 가져오기" 버튼 → 모달(`openImportModal()`)에서 CSV 파일 첨부(`onImportFileSelected`, 내용을 텍스트로 읽어와 붙여넣기 영역에 채움) 또는 텍스트 영역에 직접 붙여넣기 → "미리보기"(`previewImport()`).

**처리 과정**: 붙여넣은 내용이 비어 있으면 `alert('CSV 파일을 첨부하거나 내용을 붙여넣어줘')`로 중단한다. 값이 있으면 줄 단위로 파싱해 미리보기 표를 보여주고, 각 행이 유효한지(`valid`) 함께 표시한다.

**저장 방식**: "추가하기"(`confirmImport()`)를 누르면 `valid`한 행만 `store.transactions`에 `push`하고 `save()`를 한 번 호출한다. 마지막으로 추가된 거래의 월로 조회 월이 전환된다.

**수정**: 미리보기 표에서 행마다 날짜·카테고리·중분류·금액·구분을 다시 고칠 수 있다(`updateImportRow`). 수정할 때마다 유효성(`valid`)이 다시 계산된다. 이 수정은 아직 저장 전 임시 데이터에 대한 것이며, "추가하기"를 눌러야 실제 `store`에 반영된다.

**삭제**: 미리보기의 개별 행은 `deleteImportRow(idx)`로 확정 전에 제외할 수 있다. "다시 수정"(`backToImportInput()`)을 누르면 입력 화면으로 돌아가고, 모달을 닫으면(`closeImportModal()`) 미리보기 데이터가 전부 폐기된다.

**예외 처리**: 형식이 맞지 않는(날짜/카테고리/금액 중 하나라도 없거나 금액이 0 이하인) 행은 `valid: false`로 표시되고 "추가하기"를 눌러도 저장되지 않는다. 유효한 행이 하나도 없으면 `alert('추가할 수 있는 데이터가 없어요.')`로 중단한다. 저장이 끝나면 몇 건을 가져왔고 몇 건을 건너뛰었는지 `alert`로 안내한다.

## 8. 검색 및 필터

**목적**: 거래 내역을 원하는 조건으로 좁혀 보거나, 원하는 항목만 골라 합계를 다시 계산한다.

**입력**: 크게 두 가지 기능이다.
- 통합 검색(`txSearch` 입력창, `onTxSearch()`): 날짜·대분류·중분류·소분류·구분·금액을 하나의 문자열로 합쳐 소문자 기준 부분 일치로 찾는다.
- "필터로 합계 계산" 모드(`toggleTxFilterMode()`로 켜고 끔): 켜면 구분(`txFilterWho`)·카테고리(`txFilterCat`)·키워드(`txFilterKeyword`) 조건 입력창과 각 행 체크박스가 나타난다(`onTxFilterCriteriaChange()`).

**처리 과정**: 화면에 보이는 거래 목록은 다음 순서로 좁혀진다. ① 조회 월(전체/특정 월) → ② 가계부 보기(전체/다혜/동구/공동) → ③ 검색어 또는(필터 모드일 때) 필터 조건 → ④ 날짜순 정렬. 필터 모드가 켜져 있을 때, 조건(구분/카테고리/키워드)이나 검색어·월·가계부 보기가 바뀌면 조건에 맞는 행이 자동으로 체크되고, 사용자가 개별 체크박스(`toggleTxChecked`)나 전체 선택(`toggleAllTxChecked`)으로 다시 조정할 수 있다.

**저장 방식**: 검색어·필터 조건·체크 상태는 `store`에 저장되지 않는다. 브라우저 메모리에만 있는 값(`txSearchTerm`, `txFilterMode`, `txFilterWho`, `txFilterCat`, `txFilterKeyword`, `txCheckedExclude` 등)이라 새로고침하면 초기화된다.

**수정 / 삭제**: 해당 없음 — 검색과 필터는 데이터를 바꾸는 기능이 아니다.

**예외 처리**: 조건에 맞는 거래가 없으면 "검색 결과가 없어요" 한 줄이 표시된다.

## 9. Undo

내부 자료구조(`undoStack`, `previousStoreSnapshot`, 최대 개수, echo 시 초기화 조건)는 [DATABASE.md의 "6. Undo 구조"](DATABASE.md)에 정리돼 있으므로, 여기서는 기능적으로 어떻게 동작하는지만 다룬다.

**목적**: 방금 한 편집을 실수로 했을 때 되돌린다.

**입력**: 화면 우측 하단 되돌리기 버튼(`undoLastChange()`).

**처리 과정**: 가장 최근 변경 전 상태로 `store`를 되돌리고, 화면을 다시 그린다.

**저장 방식**: 되돌리는 동작도 `save()`를 다시 거치는 하나의 새로운 변경이라, 되돌린 결과가 그대로 Firestore에 저장된다.

**수정 / 삭제**: 해당 없음 — 되돌린 기록을 다시 고치거나 지우는 기능, "다시 실행(redo)"은 없다.

**예외 처리**: 되돌릴 변경이 없으면 버튼이 비활성화(`disabled`)되어 눌러도 아무 일도 일어나지 않는다(안내 문구는 버튼의 `title` 속성으로만 표시). 다른 기기에서 변경이 들어오면 되돌리기 기록이 초기화되므로, 그 시점 이전의 로컬 편집은 더 이상 되돌릴 수 없다.

## 10. Firestore 실시간 동기화

저장 형식, `_rev`/`lastLocalRev`를 이용한 echo 판별 등 내부 동작은 [DATABASE.md의 "1. 데이터 저장 방식"과 "5. Firestore 동기화"](DATABASE.md)에 정리돼 있다. 여기서는 사용자에게 보이는 동작만 다룬다.

**목적**: 다혜·동구가 각자의 기기에서 열어도 같은 데이터를 실시간으로 주고받는다.

**입력**: 사용자가 직접 실행하는 기능이 아니라, 1~9번 기능에서 생기는 모든 변경이 자동으로 거치는 과정이다. 동기화 자체는 페이지가 열릴 때 `initFirestore()`가 자동으로 시작한다.

**처리 과정**: 화면 상단 배지(연결 중 / 저장 중 / 저장 완료 · 실시간 공유 중 / 오류)로 현재 동기화 상태를 보여준다. 최초 연결 시 저장된 문서가 없으면 로컬의 기본 데이터를 그대로 Firestore에 올리고, 있으면 그 데이터를 불러온다.

**수정 / 삭제**: 해당 없음 — 사용자가 직접 조작하는 대상이 아니다.

**예외 처리**: 최초 연결, 저장, 실시간 수신 각각에서 오류가 나면 상단 배지가 오류 상태로 바뀌고 오류 내용이 함께 표시되며, 브라우저 콘솔에도 기록된다(`console.error`). 코드에 자동 재시도 로직은 없으므로, 연결이 끊기면 사용자가 페이지를 새로고침해야 한다.
